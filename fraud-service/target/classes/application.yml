server:
  port: 8082

spring:
  application:
    name: fraud-service
  datasource:
    url: jdbc:postgresql://localhost:5432/fraud_db
    username: postgres
    password: postgrespassword
  flyway:
    baseline-on-migrate: true
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.add.type.headers: false
core-service:
    url: http://localhost:8081

# Token compartilhado para autenticação service-to-service (deve bater com o do core-service).
internal-api:
  token: ${INTERNAL_API_TOKEN:dev-internal-shared-secret-change-me}

tracer:
  collector-url: ${TRACER_COLLECTOR_URL:http://localhost:4317}
  api-key: ${TRACER_API_KEY:}
  service-name: fraud-service
  capture-request-body: true
  capture-response-body: true

logging:
  level:
    root: INFO
    com.payflow: INFO
    org.apache.kafka: WARN
    org.springframework.kafka: WARN
